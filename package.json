{
  "name": "spam-sharing-api",
  "version": "1.0.0",
  "description": "Automates sharing posts on Facebook",
  "main": "api/spam.js", // Path to the spam.js file inside the API folder
  "scripts": {
    "start": "vercel dev",  // Local testing using Vercel
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "22.x"
  }
}

{
  "name": "spam-sharing-api",
  "version": "1.0.0",
  "description": "Automates sharing posts on Facebook",
  "main": "api/spam.js"
  "scripts": {
    "start": "vercel dev"
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "22.x"
  }
}
